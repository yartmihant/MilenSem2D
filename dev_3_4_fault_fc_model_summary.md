# Глава III.4: FC-модель с реальным материалом

## Задача

Загрузить FC-модель (заглушку из Fidesys с 75 материалами-константами) и заполнить каждый материал реальными упругими свойствами из декартовой сетки 5×5 м. Материал задан табличной зависимостью от координат (x, y) → (E, ν, ρ, демпфирование).

## Входные данные

| Файл | Описание |
|------|----------|
| `data/dev_3_3_model_stub.fc` | FC-модель с заглушками (75 материалов, скалярные значения) |
| `data/dev_3_2_fault_material.npz` | Материал на декартовой сетке (2350×560×3, E/ν/ρ) |
| `data/dev_3_1_fault_layer_boundaries.npz` | Границы 75 слоёв с разломом |

## Алгоритм

1. Загрузка декартовой сетки материала (5×5 м, 2350×560 точек)
2. Интерполяция границ слоёв (10 м → 5 м) для определения принадлежности ячеек слоям
3. Построение `layer_indexes_grid` (2350×560) — номер слоя для каждой ячейки
4. Загрузка FC-заглушки через `FCModel.load()`
5. Для каждого из 75 материалов:
   - Преобразование из скалярного свойства (`type=0`) в табличное (`type=-1, TABULAR_X + TABULAR_Y`)
   - Заполнение E, ν, ρ значениями из декартовой сетки
   - Добавление свойства MASS_DAMPING_RATIO (квадратичная рампа на краях)
6. Сохранение итоговой FC-модели

## Параметры демпфирования

- Ширина зоны поглощения: 250 м
- Максимальный коэффициент: 100
- Зоны: x < 250 м (левый край), x > 11502 м (правый край), y > 2550 м (дно)
- Форма: квадратичная рампа (d/width)²

## Выходные данные

| Файл | Описание |
|------|----------|
| `data/dev_3_4_model_calc.fc` | FC-модель для расчёта (169.2 МБ) |
| `img/dev_3_4_layers_damping.png` | Карта слоёв + зоны демпфирования |
| `img/dev_3_4_Vp_check.png` | Проверка Vp из FC-модели |

## Ключевые числа

- Материалов: 75
- Сетка: 2350×560 = 1 316 000 точек (заполнено 100%)
- Ячеек на слой: min=4707, max=156595, mean=17547
- E: 2.06e+09 .. 5.55e+10 Па
- Vp: ~1200 .. ~5300 м/с
