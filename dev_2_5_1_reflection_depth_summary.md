# II.5.1. Оценка глубины отражения для источника x = 6000 м

> Исторический этап главы II.5. Итоговые исправленные узловые SEG-Y и FC описаны в [главе II.5](dev_2_5_material_consistency_summary.md). Числа этого этапа относятся к прежнему `legacy`-комплекту; они не являются метриками окончательной пары.

> **Первоначальная интерпретация отозвана после проверки точного FC и загрузчика.** Этот документ сохраняет ход предварительной оценки по SEG-Y. Расчётная область заканчивается на 2650 м, а не на 2750 м. Текущий загрузчик SEMGPU ошибочно превращает таблицы в константы слоёв, поэтому перехода внутри слоя 75 на 2490–2500 м в таком расчётном материале нет. Актуальная привязка — границы 73/74 и 74/75 на 2401.84 и 2411.84 м, с временами 2.1546 и 2.1604 с. См. [аудит FC/SEG-Y и загрузчика](dev_2_5_2_fc_segy_audit_summary.md) и [актуальный рисунок](img/dev_2_5_2_fc_segy_audit_x6000.png).

## Результат

Наиболее подходящий **локальный кандидат по времени** для яркого пакета Vy
около 2.15 с — переход свойств на **2490–2500 м внутри 75-го слоя**
(кристаллический сланец). Это предварительная привязка: прямое интегрирование
не доказывает тип волны и не исключает кратные отражения или двумерные эффекты.

На трассе x=6000 м сильные экстремумы пакета находятся на 2.146, 2.160 и
2.174 с. Перевод первых двух времён в глубину даёт 2511 и 2541 м, если
формально считать их временем максимума исходного давления. Разные фазы
одного пакета нельзя интерпретировать как отдельные отражающие границы.
Практическая оценка глубины — **около 2.5 км**, с неопределённостью в десятки
метров, а не точная отметка по одному экстремуму.

## Исходные файлы

- `data/dev_2_3/x6000_seismogram_vy.png` — визуальное выделение события.
- `data/dev_2_3/x6000_data.npz` — времена, координаты и компоненты Vy/Vx;
  2001 отсчёт через 0.002 с, 1176 приёмников через 10 м.
- `data/dev_2_1_Vp_model.sgy`, `data/dev_2_1_Vs_model.sgy`,
  `data/dev_2_1_Density_model.sgy` — профили материала.
- `data/dev_1_7_material_grids.npz` — физические координаты центров ячеек
  и номера геологических слоёв.
- `data/dev_1_5_2_layer_boundaries_quadratic.npz` — геометрия 75 слоёв
  и названия пород.
- `data/dev_1_2_profile_sections_depths.npz` — исходные сейсмические горизонты.
- `dev_2_1_segy_material.py` — проверка единиц плотности и правил экспорта SEG-Y.
- `/home/antonov/Base/Research/TrySEMGPU/research/seismic/test_exp_seismic_sweep.py`
  — предоставленный пользователем точный скрипт моделирования: объёмный источник
  давления `P2_VOLUME_PRESSURE`, глубина 5 м, радиус 5 м, частота 30 Гц,
  длительность таблицы 0.20 с, 256 табличных отсчётов; материал из FC.
- `/home/antonov/Base/Research/TrySEMGPU/semgpu/src/semgpu/solvers/tools/builders/conditions/lin_elast_2d/lamb.py`
  — функция `build_ricker`: максимум при `t0 = 1/f0`, а не при половине
  длительности таблицы. Последние два файла только прочитаны; параметры
  зафиксированы в дневнике, повторный запуск не импортирует TrySEMGPU.

## Метод и поправки

Для почти нулевого удаления источник–приёмник используем вертикальную PP-траекторию:

\[
T_P(z)=\int_0^z\frac{d\zeta}{V_P(6000,\zeta)},\qquad
t_{PP}(z)=0.0333333+2T_P(z)-T_P(5).
\]

Интегрируется **медленность 1/Vp**. Каждый отсчёт представляет ячейку толщиной
5 м. Из источника на глубине 5 м нисходящий путь короче восходящего;
поэтому вычитается один участок `T_P(5) = 0.00279287 с`.
Суммарная поправка к двойному времени от поверхности: **+0.03054046 с**.

Существующие SEG-Y содержат значения центров ячеек, но их заголовки смещены
на −2.5 м. Трасса с CDP_X=6000 физически относится к x=6002.5 м. Интегрирование
выполнено по границам ячеек 0–2750 м. Проверка соседнего столбца x=5997.5 м
даёт максимальное различие PP-времени по всему разрезу всего **0.082 мс**.
Плотность переведена из г/см³ в кг/м³.

Рассчитаны `Zp = rho*Vp` и нормальные коэффициенты
`R = (Zp_below-Zp_above)/(Zp_below+Zp_above)`. Это индикаторы отражающей
способности; они не учитывают передачу через вышележащие границы, интерференцию,
геометрическое расхождение и преобразование давления в регистрируемую Vy.
Описание метода: [учебные материалы UBC](https://www.eoas.ubc.ca/courses/eosc350/content/methods/meth_10d/normal.html).

## Сопоставление кандидатов

- **Граница 73/74**, сланец глинистый → аргиллит: 2401.68 м,
  расчётное время **2.09310 с**. В растре переход на 2400 м:
  Vp 3243 → 3648 м/с, плотность 2.2383 → 2.3142 г/см³, R ≈ +0.0754.
- **Граница 74/75**, аргиллит → сланец кристаллический: 2411.68 м,
  время **2.09843 с**. В растре переход на 2410 м:
  Vp 3712 → 4150 м/с, плотность 2.3241 → 2.3998 г/см³, R ≈ +0.0717.
  Эти два близких сильных контраста должны рассматриваться как интерферирующая
  группа. На исходной трассе около 2.10 с имеется более слабый пакет;
  временная близость сама по себе ещё не доказывает его происхождение.
- **Нижний исходный горизонт A**: 2415.77 м, время **2.10040 с**.
  Это геометрический ориентир, не дополнительный доказанный скачок импеданса:
  окончательные границы слоёв и исходные горизонты не совпадают строго.
- **Переход внутри слоя 75**, около 2490–2500 м:
  время **2.13632–2.14101 с**. Между центрами 2482.5 и 2502.5 м Vp
  возрастает примерно с 4058 до 4373 м/с, плотность — с 2.3848 до
  2.4343 г/см³. Суммарное увеличение импеданса около 10%, эквивалентный
  коэффициент резкой границы около +0.048. В растре переход распределён
  по нескольким ячейкам; самые сильные локальные R ≈ +0.0181 и +0.0179
  на 2490 и 2495 м. Все эти точки имеют номер слоя 75.
- **Более глубокий переход около 2670 м**: время **2.21909 с**.
- **Техническое дно на 2750 м**: время **2.25419 с**. Оно позднее
  выделенного пакета; по SEG-Y нельзя оценить коэффициент отражения от
  вычислительной границы. В скрипте моделирования заданы поглощающие условия.

Без учёта источника широкое окно 2.1–2.2 с даёт 2478–2696 м;
**с учётом источника — 2415–2628 м**. Само широкое окно не позволяет
различить границу последних слоёв и переход внутри последнего слоя.
Для яркого пакета около 2.15 с локально лучше подходит второй кандидат.

Разность 5–24 мс между расчётной привязкой перехода 2490–2500 м и двумя
сильнейшими экстремумами Vy явно сохраняется. Она сопоставима с масштабом
импульса 30 Гц, но её происхождение здесь не установлено. Для однозначного
вывода нужны фазовая привязка отклика и проверка двумерных лучей/волнового
поля либо контрольный расчёт с удалением данного контраста. Фактическое
поле FC из указанного запуска не было повторно интегрировано; расчёт глубины
использует заданные пользователем SEG-Y.

## Выходные файлы и проверка

- `dev_2_5_1_reflection_depth.py` — воспроизводимый императивный дневник.
- `data/dev_2_5_1_reflection_depth.npz` — профили Vp/Vs/плотности,
  импеданс, коэффициенты отражения, PP/PS/SS-времена, номера слоёв,
  горизонты, кандидаты, трассы Vy/Vx и параметры источника.
- `data/dev_2_5_1_reflection_depth.json` — основные численные результаты.
- `img/dev_2_5_1_reflection_depth.png` — профили скорости и импеданса,
  глубинно-временная привязка и фрагмент трассы Vy.
- `dev_2_5_1_reflection_depth_summary.md` — настоящий реферат.

Дневник полностью выполнен командой
`MPLBACKEND=Agg .venv/bin/python dev_2_5_1_reflection_depth.py`;
рисунок проверен визуально. `py2jupyter` в окружении не установлен,
поэтому цикл конвертации не проверялся. Файлы `.ipynb` и исходные данные
не читались и не изменялись.
